// helpers.js
function randomChoice(choices) {
    const randomIndex = Math.floor(Math.random() * choices.length);
    return choices[randomIndex];
  }
  
  function determineWinner(userChoice, computerChoice) {
    if(user)
    // Implementa la lógica para determinar el ganador aquí
    // Devuelve un mensaje indicando si el usuario ganó, perdió o hubo empate
  }
  
module.exports = { randomChoice, determineWinner };  